(()=>{const app=document.querySelector('#app');const token=location.pathname.split('/').filter(Boolean).pop();const icons=()=>window.lucide?.createIcons({attrs:{'stroke-width':1.8}});const escape=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const typeIcon={text:'file-text',video:'play-circle',pdf:'file-text',exercise:'dumbbell',question:'message-circle',checklist:'list-checks',photo:'camera',link:'link'};async function api(path,opts={}){const res=await fetch(`/api/v1/patient-access/${token}${path}`,{...opts,headers:{'Content-Type':'application/json'}});if(!res.ok)throw new Error((await res.json().catch(()=>({}))).detail||'Ссылка недействительна');return res.status===204?null:res.json()}function card(item,kind){const done=item.completed;const question=item.question?`<div class="question"><label for="answer-${kind}-${item.id}">${escape(item.question)}</label><textarea id="answer-${kind}-${item.id}" placeholder="Ваш ответ"></textarea></div>`:'';return `<article class="card ${done?'done':''}"><div class="item-head"><span class="kind"><i data-lucide="${typeIcon[item.block_type]||'pill'}"></i></span><div class="item-title"><strong>${escape(item.title||item.medication_name)}</strong><span>${escape(item.content||item.dosage||'')}</span></div>${done?'<span class="check"><i data-lucide="check"></i></span>':''}</div>${item.resource_url?`<a href="${escape(item.resource_url)}" target="_blank" rel="noreferrer">Открыть материал</a>`:''}${question}${!done?`<button class="button" data-kind="${kind}" data-id="${item.id}"><i data-lucide="check"></i>Отметить выполненным</button>`:''}</article>`}function render(data){const p=data.total_count?Math.round(data.completed_count/data.total_count*100):0;app.innerHTML=`<header class="hero"><div class="topline"><span>Nepres Clinic</span><span>День ${data.day_number}${data.duration_days?' из '+data.duration_days:''}</span></div><h1>Здравствуйте, ${escape(data.patient_name.split(' ')[0])}</h1><p>${escape(data.plan_title)}</p><div class="progress-row"><div class="ring" style="--progress:${p}%"><span>${p}%</span></div><div class="progress-copy"><strong>Ваш прогресс сегодня</strong><small>${data.completed_count} из ${data.total_count} пунктов выполнено</small></div></div></header>${data.medications.length?`<section><h2 class="section-title">Лекарства</h2><div class="cards">${data.medications.map(m=>`<article class="medicine ${m.completed?'done':''}"><span class="medicine-time">${escape(m.scheduled_time||'В течение дня')}</span><div class="medicine-copy"><strong>${escape(m.medication_name)}</strong><small>${escape(m.dosage||'')}</small></div>${m.completed?'<span class="check"><i data-lucide="check"></i></span>':`<button class="check" data-kind="medication" data-id="${m.id}" aria-label="Отметить приём"><i data-lucide="check"></i></button>`}</article>`).join('')}</div></section>`:''}<section><h2 class="section-title">На сегодня</h2><div class="cards">${data.blocks.map(b=>card(b,'block')).join('')||'<div class="empty">На сегодня заданий нет.</div>'}</div></section>`;icons()}app.addEventListener('click',async e=>{const b=e.target.closest('[data-kind]');if(!b)return;b.disabled=true;const answer=document.querySelector(`#answer-${b.dataset.kind}-${b.dataset.id}`)?.value||null;try{await api(`/${b.dataset.kind}/${b.dataset.id}/complete`,{method:'POST',body:JSON.stringify({answer})});render(await api('/today'))}catch(err){alert(err.message);b.disabled=false}});api('/today').then(render).catch(e=>{app.innerHTML=`<section class="error"><h1>План недоступен</h1><p>${escape(e.message)}</p></section>`;icons()})})();
